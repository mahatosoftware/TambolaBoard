<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- For a solid color background -->
        <item name="android:windowBackground">@color/purple</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowNoTitle">true</item>
        <!-- For an image background (uncomment and replace if using an image) -->
        <!-- <item name="android:windowBackground">@drawable/my_background_image</item> -->
    </style>

    <style name="TambolaNumberText">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
        <item name="android:background">@color/dark_purple</item>

    </style>

    <style name="TambolaBoardHomeHeader">
        <item name="android:textColor">@color/white</item>


    </style>

    <style name="TambolaNumberBackground">

        <item name="android:background">@color/dark_purple</item>

    </style>

</resources>